import { Modal } from '@/components/ui/Modal';
import { StubPage } from '@/components/ui/StubPage';

export default function StartSessionPage() {
  return (
    <Modal>
      <StubPage title="StartSessionPage" />
    </Modal>
  );
}
